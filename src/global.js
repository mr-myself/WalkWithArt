import { Dimensions } from 'react-native';

module.exports = {
    BASE_URL: 'http://localhost:3000',
    DEVICE_WIDTH: Dimensions.get('window').width,
    DEVICE_HEIGHT: Dimensions.get('window').height,
    AD_UNIT_ID: 'AD_UNIT_ID',
    TEST_DEVICE: 'EMULATOR'
}
